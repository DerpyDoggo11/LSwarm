
[platformio]
default_envs = drone
boards_dir = boards

; ---------------------------------------------------------------------------
[env]
platform = https://github.com/maxgerhardt/platform-raspberrypi.git
board = rp2350
framework = arduino
board_build.core = earlephilhower

board_build.filesystem_size = 1m

monitor_speed = 115200
build_flags =
    -DUSB_MANUFACTURER='"LSwarm"'
    -DUSB_PRODUCT='"Mk.1"'
    -DPICO_XOSC_STARTUP_DELAY_MULTIPLIER=64
    -O2

lib_deps =
    adafruit/Adafruit NeoPixel @ ^1.12.3

[env:drone]
; EXTERNAL_ESP: on-board ESP32-C3 bypassed, external ESP32-S3 on the header.
; Holds the C3 in reset so it can't fight the shared UART. Drop it if you ever
; go back to the on-board C3.
build_flags = ${env.build_flags} -DEXTERNAL_ESP=1

[env:esp_passthrough]
build_flags = ${env.build_flags} -DESP_PASSTHROUGH_ONLY=1

[env:drone_debug]
build_flags = ${env.build_flags} -DDEBUG_BUILD=1 -g -Og
build_type = debug
upload_protocol = cmsis-dap
debug_tool  = cmsis-dap